import styled from 'styled-components'

const Title = styled.div`
	font-size: 4rem;
	font-family: 'Italianno', cursive;
	color: white;
	position: absolute;
	top: 50%;
	left: 50%;
`

export default function Top() {
	return <Title>fill 's portfolio</Title>
}
